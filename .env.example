# 可选：构建桌面端时覆盖默认 manifest 地址（编译期注入到 Rust）
# ARENA_MANIFEST_URL=https://raw.githubusercontent.com/<owner>/<repo>/main/data/manifest.json

# 可选：本地跑抓取脚本时覆盖 Release 下载地址模板
# ARENA_DATA_URL_TEMPLATE=https://github.com/<owner>/<repo>/releases/download/{version}/arena-data.json.gz
